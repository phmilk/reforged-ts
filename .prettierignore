# Build outputs.
**/dist/
packages/reforged-types/build/
packages/reforged-test/lua/
packages/reforged-ts/dist-test/
release/build/
wrapper-coverage/build/

# What tools own: the Overlay JSON files, the generated Typings, the vendored
# Patch files, the record the seed script writes and the pnpm lockfile.
packages/reforged-types/overlay/
packages/reforged-types/3.0.0/
packages/reforged-types/3.0.0.d.ts
packages/reforged-types/vendor/
packages/reforged-types/scripts/war3-types-strict.json
pnpm-lock.yaml

# The compatibility matrix, written byte for byte by release:matrix.
release/compatibility/matrix.json
release/compatibility/matrix.md

# The Wrapper coverage report, written byte for byte by coverage:report.
wrapper-coverage/report.json
wrapper-coverage/report.md

# Lua is not formatted.
*.lua

# The VS Code files stay as they are until the DX step (#41).
.vscode/

# Agent worktrees and local state.
.claude/
