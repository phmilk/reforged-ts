---
---

The docs site generates the Typings' reference at every build: under API, Typings, one subsection per Game version `reforged-types` ships, with a page per Native, Blizzard.j function and global showing its signature, its Jass types and its `Async` marker. Nothing is published.
