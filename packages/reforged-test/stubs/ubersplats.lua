-- reforged-test stubs for the ubersplats family.

function CreateUbersplat(x, y, name, red, green, blue, alpha, forcePaused,
                         noBirthTime)
  __stub_record("CreateUbersplat", x, y, name, red, green, blue, alpha,
    forcePaused, noBirthTime)
  return __stub_new_handle("ubersplat")
end
