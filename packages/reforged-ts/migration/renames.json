[
  {
    "old": "w3ts",
    "new": "reforged-ts",
    "kind": "package",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The library is published under a new name: an import from `w3ts` reads the same exports from `reforged-ts`, less the symbols the other entries rename or remove."
  },
  {
    "old": "new CameraSetup(...)",
    "new": "CameraSetup.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `CameraSetup.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Destructable(...)",
    "new": "Destructable.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The constructor took the rawcode, x, y, z, facing, scale and variation in that order; `Destructable.create` takes one options object, `{ typeId, x, y, z, face, scale, variation }`, and picks the creation Native from the options given."
  },
  {
    "old": "Destructable.createZ",
    "new": "Destructable.create",
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "Removed: `Destructable.create` takes the z coordinate as its `z` option, `Destructable.create({ typeId, x, y, z })`, next to the facing, scale, variation and skin `createZ` took in order."
  },
  {
    "old": "new Dialog(...)",
    "new": "Dialog.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Dialog.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new DialogButton(...)",
    "new": "DialogButton.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `DialogButton.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Effect(...)",
    "new": ["Effect.create(...)", "Effect.createAttachment(...)"],
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The overloads split by argument shape: `(modelName, x, y)` is `Effect.create` and `(modelName, targetWidget, attachPointName)` is `Effect.createAttachment`, each with the same arguments."
  },
  {
    "old": "new FogModifier(...)",
    "new": "FogModifier.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `FogModifier.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Force(...)",
    "new": "Force.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Force.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Frame(...)",
    "new": [
      "Frame.create(...)",
      "Frame.createSimple(...)",
      "Frame.createType(...)"
    ],
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The overloads split by argument shape: `(name, owner, priority, createContext)` is `Frame.create` with the same arguments, `(name, owner, priority)` is `Frame.createSimple` with the same arguments, and `(name, owner, priority, createContext, typeName, inherits)` is `Frame.createType(name, owner, createContext, typeName, inherits)`, which drops the priority the constructor ignored."
  },
  {
    "old": "new GameCache(...)",
    "new": "GameCache.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `GameCache.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Group(...)",
    "new": "Group.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Group.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Image(...)",
    "new": "Image.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Image.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Item(...)",
    "new": "Item.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Item.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Leaderboard(...)",
    "new": "Leaderboard.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Leaderboard.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new MapPlayer(...)",
    "new": "MapPlayer.fromIndex(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "Players are not created, so the constructor, private in w3ts 3.x, gives way to the lookup `MapPlayer.fromIndex`, which keeps the index argument but returns `MapPlayer | undefined`, `undefined` for a slot out of range, so the call site must handle the missing player."
  },
  {
    "old": "new Multiboard(...)",
    "new": "Multiboard.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Multiboard.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new MultiboardItem(...)",
    "new": "MultiboardItem.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `MultiboardItem.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Point(...)",
    "new": "Point.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Point.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Quest(...)",
    "new": "Quest.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Quest.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new QuestItem(...)",
    "new": "QuestItem.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `QuestItem.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Rectangle(...)",
    "new": "Rectangle.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Rectangle.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Region(...)",
    "new": "Region.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Region.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Sound(...)",
    "new": "Sound.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Sound.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new TextTag(...)",
    "new": "TextTag.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `TextTag.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Timer(...)",
    "new": "Timer.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Timer.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new TimerDialog(...)",
    "new": "TimerDialog.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `TimerDialog.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Trigger(...)",
    "new": "Trigger.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Trigger.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Ubersplat(...)",
    "new": "Ubersplat.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Ubersplat.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new Unit(...)",
    "new": "Unit.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `Unit.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "new WeatherEffect(...)",
    "new": "WeatherEffect.create(...)",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The deprecated constructor is gone: `WeatherEffect.create` takes the same arguments and is the one way to create the Handle, throwing when the game returns nothing."
  },
  {
    "old": "Frame.parent",
    "new": ["Frame.getParent", "Frame.setParent"],
    "kind": "accessor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The deprecated accessor splits into `getParent()`, which returns `undefined` when the frame has no parent, and `setParent(parent)`."
  },
  {
    "old": "Unit.owner",
    "new": ["Unit.getOwner", "Unit.setOwner"],
    "kind": "accessor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The deprecated accessor splits into `getOwner()` and `setOwner(player)`, whose `changeColor` argument defaults to `true` as the setter did."
  },
  {
    "old": "Unit.point",
    "new": ["Unit.getPoint", "Unit.setPoint"],
    "kind": "accessor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The deprecated accessor splits into `getPoint()`, which creates a new location on each call, and `setPoint(point)`."
  },
  {
    "old": "Group.getEnumUnit",
    "new": "Unit.fromEnum",
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The enumerated unit has one accessor, on the class it returns, instead of two names for one Native."
  },
  {
    "old": "Group.getFilterUnit",
    "new": "Unit.fromFilter",
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The filtered unit has one accessor, on the class it returns, instead of two names for one Native."
  },
  {
    "old": "MapPlayer.create",
    "new": "MapPlayer.fromIndex",
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "Players are not created, so the factory, private in w3ts 3.x, gives way to the lookup `MapPlayer.fromIndex`, which returns `undefined` for a slot out of range."
  },
  {
    "old": "Handle.getObject",
    "new": "Handle.fromHandle",
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The protected wrapping step of a Wrapper subclass gives way to the inherited `fromHandle`, typed on the class it is called on and returning `undefined` for an undefined Handle, while a creation member returns `this.expect(Native(...), detail)` instead."
  },
  {
    "old": "Handle.initFromHandle",
    "new": null,
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "No constructor asks whether it is wrapping an existing Handle any more: the protected constructor takes the Handle and only stores it, and the base registers the Wrapper."
  },
  {
    "old": "Trigger.registerTimerExpireEvent",
    "new": "Trigger.registerTimerExpire",
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The registration takes the `Timer` Wrapper instead of the raw `timer` handle, like the Trigger's other registrations, so the argument `timer.handle` becomes `timer`."
  },
  {
    "old": "Trigger.triggerRegisterFrameEvent",
    "new": "Trigger.registerFrameEvent",
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The one registration whose name began with `trigger` takes the name of its siblings, with the same `Frame` and `frameeventtype` arguments."
  },
  {
    "old": "Trigger.registerTrackableHitEvent",
    "new": "Trigger.registerTrackableHit",
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The registration takes the new `Trackable` Wrapper, which `Trackable.create` returns, instead of the raw `trackable` handle."
  },
  {
    "old": "Trigger.registerTrackableTrackEvent",
    "new": "Trigger.registerTrackableTrack",
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The registration takes the new `Trackable` Wrapper, which `Trackable.create` returns, instead of the raw `trackable` handle."
  },
  {
    "old": "Trigger.registerPlayerMouseEvent",
    "new": "Trigger.registerPlayerMouseEvent",
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "Not a rename: the second argument is `MouseEventKind.Down`, `MouseEventKind.Up` or `MouseEventKind.Move` instead of Blizzard.j's number (`bj_MOUSEEVENTTYPE_DOWN`, `_UP`, `_MOVE`), and a number is now a type error."
  },
  {
    "old": "main::before",
    "new": "Init.onGlobals",
    "kind": "entryPoint",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "`addScriptHook(W3TS_HOOK.MAIN_BEFORE, hook)` becomes `Init.onGlobals(hook)`, which runs later, after `InitGlobals`, by design: nothing should be created before the map's globals exist, and the library's own setup (`Players`, the sync Trigger) runs first in that stage."
  },
  {
    "old": "main::after",
    "new": "Init.onInitTriggers",
    "kind": "entryPoint",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "`addScriptHook(W3TS_HOOK.MAIN_AFTER, hook)` becomes `Init.onInitTriggers(hook)`, the same moment: `RunInitializationTriggers` is the last call of `main`, so the stage runs at the end of `main`."
  },
  {
    "old": "config::before",
    "new": null,
    "kind": "entryPoint",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "No Init stage runs in the lobby, before `config`, in the first release: code that needs lobby timing stays on `addScriptHook`, deprecated and removed in 2.0, until a lobby stage exists."
  },
  {
    "old": "config::after",
    "new": null,
    "kind": "entryPoint",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "No Init stage runs in the lobby, after `config`, in the first release: code that needs lobby timing stays on `addScriptHook`, deprecated and removed in 2.0, until a lobby stage exists."
  },
  {
    "old": "W3TS_HOOK",
    "new": null,
    "kind": "type",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The enum of `addScriptHook` is deprecated with it and still exported for this one release; its four values are the entry points listed here, each with its stage or none, and both are removed in 2.0."
  },
  {
    "old": "hookedMain",
    "new": null,
    "kind": "function",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "An implementation detail of the old Hook code: the Init machinery installs the wrapper of `main` itself, in place or on the function's first assignment, and no wrapper has a name."
  },
  {
    "old": "hookedConfig",
    "new": null,
    "kind": "function",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "An implementation detail of the old Hook code: the Init machinery installs the wrapper of `config` itself, in place or on the function's first assignment, and no wrapper has a name."
  },
  {
    "old": "executeHooksMainBefore",
    "new": null,
    "kind": "function",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "An implementation detail of the old Hook code: the wrapper of `main` runs the `main::before` queue, each hook under `pcall`, and nothing runs it by hand."
  },
  {
    "old": "executeHooksMainAfter",
    "new": null,
    "kind": "function",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "An implementation detail of the old Hook code: the wrapper of `main` runs the `main::after` queue, each hook under `pcall`, and nothing runs it by hand."
  },
  {
    "old": "executeHooksConfigBefore",
    "new": null,
    "kind": "function",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "An implementation detail of the old Hook code: the wrapper of `config` runs the `config::before` queue, each hook under `pcall`, and nothing runs it by hand."
  },
  {
    "old": "executeHooksConfigAfter",
    "new": null,
    "kind": "function",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "An implementation detail of the old Hook code: the wrapper of `config` runs the `config::after` queue, each hook under `pcall`, and nothing runs it by hand."
  },
  {
    "old": "new SyncRequest(...)",
    "new": "SyncRequest.send",
    "kind": "constructor",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "Not a rename: the constructor takes only the sender and the options and never starts a request, so the overloads that took the data and started at once are gone, and `new SyncRequest(from, data, options)` becomes `SyncRequest.send(from, data, options)`."
  },
  {
    "old": "SyncRequest.then",
    "new": ["SyncRequest.start", "SyncRequest.send"],
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "`then` stored one callback and returned the request, which `await` could not use; `start(data)` and `SyncRequest.send` return a real `Promise` of the `SyncResponse`, so `request.then(callback)` becomes `request.start(data).then(callback)` or an `await`."
  },
  {
    "old": "SyncRequest.catch",
    "new": ["SyncRequest.start", "SyncRequest.send"],
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The `Promise` that `start(data)` and `SyncRequest.send` return rejects instead, with a string naming the request and the cause: a timeout, a cancellation or a network error."
  },
  {
    "old": "SyncCallback",
    "new": null,
    "kind": "type",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The callback type of `then` and `catch` goes with them: a `Promise` continuation receives the `SyncResponse`, which carries the request."
  },
  {
    "old": "ISyncResponse",
    "new": "SyncResponse",
    "kind": "type",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The type drops the `I` prefix and is read-only; it gains the sender, `from`, and the `request`, and loses `status`, which the request's own `status` holds."
  },
  {
    "old": "ISyncOptions",
    "new": "SyncOptions",
    "kind": "type",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "The type drops the `I` prefix; its `timeout` is optional and read-only, zero or absent meaning no timeout."
  },
  {
    "old": "SyncRequest.destroy",
    "new": "SyncRequest.cancel",
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": true,
    "note": "Named for what it does: it rejects a pending request with a cancellation reason and does nothing on a settled one; ids are never recycled, so there is nothing to release."
  },
  {
    "old": "SyncRequest.fromIndex",
    "new": null,
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The table of pending requests is private: the System matches a packet to its request by id itself, and a continuation receives the request in the `SyncResponse`."
  },
  {
    "old": "onHostDetect",
    "new": "Host.detectHost",
    "kind": "function",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The callback gives way to a `Promise` of the elected `MapPlayer`, the same `Promise` for every call, and `Host.host` reads the result once it resolved; nothing is detected until `detectHost` is called."
  },
  {
    "old": "BinaryReader.read",
    "new": null,
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The generic read that took a format and a size, the size the source of the `readDouble` misalignment, is private: the typed `readX` members advance by what `string.unpack` consumed."
  },
  {
    "old": "BinaryReader.data",
    "new": null,
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The binary string read is private: the typed `readX` members read it, and `position` and `remaining` tell how far they got."
  },
  {
    "old": "BinaryWriter.values",
    "new": null,
    "kind": "member",
    "versions": {
      "from": "w3ts@3",
      "to": "reforged-ts@1"
    },
    "oneToOne": false,
    "note": "The array of values written so far is private: `toString()` packs them, and the typed `writeX` members are the only way to add one."
  }
]
