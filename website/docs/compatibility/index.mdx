---
title: Compatibility
description: The versions of the packages, the game Patch and the Toolchain that work together, one row per release.
---

import Matrix from "./_matrix.mdx";

# Compatibility

Which versions of the four packages, which game Patch and which Toolchain go together: one row per stable release, newest first, generated from the packages at each release and never edited by hand.

<Matrix />
