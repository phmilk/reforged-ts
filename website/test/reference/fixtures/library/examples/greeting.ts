// The example greet's doc comment includes: only the region is shown.
import { greet } from "../src/index";

// #region greet
const greeting = greet("Arthas");
// #endregion greet

export { greeting };
